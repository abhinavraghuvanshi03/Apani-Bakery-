- name: Extract app project
        run: |
          unzip -o apni_bakery_flutter_app.zip -d app
          ls -la app

      - name: Replace main.dart with fixed version
        run: cp main.dart app/lib/main.dart
